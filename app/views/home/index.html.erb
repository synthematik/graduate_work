
<div>
  <div class="grid grid-cols-12 max-h-screen">
    <div class="flex flex-col gap-8 p-6 pt-0 col-span-7">
      <div class="flex justify-center align-middle">
      </div>
      <% @articles.each do |article| %>
        <div class="p-3 border border-opacity-30 border-gray-300 rounded-2xl bg-cyan-100/10">
          <h1 class="text-2xl text-cyan-700/80"><%= article.title %></h1>
          <div class="flex gap-6 text-gray-600 ">
            <h2>Автор: <%= article.user.email %></h2>
            <h2>Опубликовано: <%= article.created_at.strftime("%d.%m.%Y") %></h2>
          </div>
          <div class="flex justify-center w-full">
            <button class="text-gray-500" >Подробнее</button>
          </div>
        </div>
      <% end %>
    </div>
    <div class="col-span-4 flex flex-col gap-4 mt-8 border border-opacity-30 border-gray-300 rounded-2xl bg-gray-100/10 p-3 max-h-screen">
      <h2 class="text-center font-bold text-cyan-700/70 text-xl ">Объявления</h2>
      <div class="flex flex-col gap-5 border-t-2 border-b-2 border-gray-300 overflow-y-auto py-2 divide-y divide-gray-400 divide-dashed">
        <% @articles.each do |article| %>
          <div class="flex justify-between pb-5 align-middle">
            <a class="hover:text-cyan-400" href="/"><%= article.body %></a>
            <p class="text-gray-400/70"><%= article.created_at.strftime("%d.%m.%Y") %></p>
          </div>
        <% end %>
      </div>
      <h2 class="text-center font-bold text-cyan-700/70 text-xl ">Как нас найти </h2>
      <div class="px-4 border-t-2 border-b-2 border-gray-300 py-2">
        <p>Мехмат ЮФУ</p>
        <p>344090 Ростов-на-Дону ул. Мильчакова 8a</p>
        <a class="hover:text-cyan-400" href="/">Найти на карте</a>
        <p> +7 (863) 2975 111</p>
        <p> Факс: +7 (863) 2975 112</p>
        <p>Деканат: кабинет 111</p>
      </div>
    </div>
  </div>
</div>
